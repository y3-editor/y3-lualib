---
alwaysApply: true
---
# Y3 项目规则

Y3 游戏编辑器地图工程，使用 **y3-lualib** 框架。

> 本文件为核心规则索引，详细规则见各主题文件。

## � 规则文件索引

| 文件 | 用途 |
|------|------|
| `rules.mdc` | 核心规则索引（本文件） |
| `memory.mdc` | 记忆系统规则 |
| `api-safety.mdc` | API 安全规则、常见 API 错误 |
| `mcp-rules.mdc` | MCP 调用规则、热更保存流程 |
| `ui-rules.mdc` | UI 开发规则、坐标系、官方组件 |

## 🚀 首次会话环境检查

```
1. 检查 .codemaker/env_setup_done 是否存在
   ├─ 存在 → ✅ 跳过
   └─ 不存在 → 检查 y3-lualib

2. 检查 y3-lualib（任一路径存在即可）
   - maps/EntryMap/script/y3/init.lua
   - global_script/y3/init.lua
   ├─ 存在 → ✅ 跳过
   └─ 不存在 → 激活 y3-env-setup
```

## 🎯 技能路由

| 用户需求 | 技能 |
|----------|------|
| 做一个游戏 | `y3-game-spec` |
| 生成物编 | `y3-obj-gen` |
| 修改物编 | `y3-obj-edit` |
| UI/界面 | `y3-ui-pipeline` ⭐ |
| Lua 代码 | `y3-lua-pipeline` |
| 自动化测试 | `desktop-automation` |

> ⭐ 所有 UI 需求走 `y3-ui-pipeline`，内部自动路由到子技能。
> ⭐ 所有 Lua 需求走 `y3-lua-pipeline`，内部自动路由到子技能。

## 🔍 开发前检查（强制）

| 任务类型 | 必须先阅读 |
|---------|-----------|
| Lua 代码 | `lua-issues/` + `references/` |
| UI 开发 | `knowledge/08-官方UI组件.md` |
| 物编操作 | 物编知识 + 模板文件 |

## ⚠️ 核心禁令

| 禁止 | 正确做法 |
|------|----------|
| 臆造 API | 查阅 `y3/` 源码验证 |
| 手写大型 JSON | 使用脚本生成 |
| Lua 实现引擎功能 | 使用物编配置 |
| 手写技能栏/Buff/物品栏 | 使用官方组件 type:17/18/20 |
| UI 坐标用左上角思维 | **Y3 原点在左下角** |
| 物编/UI 生成后直接保存 | **必须先热更再保存** |
| 有现成脚本还手写 | 先查 pipeline/ 目录 |

> 详细禁令说明见 `api-safety.mdc`、`ui-rules.mdc`、`mcp-rules.mdc`

## 🎮 Y3 引擎速查

### 玩家编号

| 编号 | 用途 |
|------|------|
| 1-12 | 普通玩家 |
| 31 | 中立敌对（怪物） |
| 32 | 中立友好（NPC） |

### 键盘常量

```lua
-- ✅ 正确
y3.const.KeyboardKey['KEY_1']

-- ❌ 错误
y3.const.KeyboardKey['1']
```

## 📚 知识库

| 文档 | 说明 |
|------|------|
| `knowledge/` | Y3 引擎知识库 |
| `skills/*/SKILL.md` | 技能详细指南 |
| `skills/*/references/` | API 速查和代码片段 |
| `memory/lua-issues/` | 错题本（API 和 Trace） |

## 🖱️ 桌面自动化

```
1. 获取控件坐标 → y3editor MCP
2. 移动鼠标 → bot_move_mouse（支持负坐标）
3. 点击 → PowerShell 管理员脚本
```

> 详细流程见 `desktop-automation/SKILL.md`

---

*最后更新: 2026-04-03*