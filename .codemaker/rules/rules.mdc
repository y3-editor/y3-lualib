---
alwaysApply: true
---
# Y3 项目规则

Y3 游戏编辑器地图工程，使用 **y3-lualib** 框架。

## � 整体开发流程

**整体开发、测试流程参考 `y3-game-spec` 技能。**

当涉及游戏功能开发时，遵循 `y3-game-spec` 的工作流：
1. 明确功能需求 → 生成测试文档
2. 物编生成 → UI 生成 → Lua 代码
3. 测试验证 → 问题追踪 → 迭代修复

## �🎯 技能系统

处理请求前先查阅 `.codemaker/skills/README.md` 匹配技能，加载对应 `SKILL.md` 执行。

## 🔍 开发前置检查（强制）

**在开始编写代码或生成物编/UI之前，必须先检查以下内容：**

### 1. 错题集检查
根据当前任务类型，阅读 `.codemaker/memory/lua-issues/` 中的相关记录：
- Lua 代码开发 → 查阅 `trace_issues.md`（报错/堆栈问题）
- API 使用相关 → 查阅 `api_issues.md`（API 参数/类型问题）

### 2. 知识库检查
根据任务涉及的功能，阅读对应知识库文档：
- UI 开发 → `.codemaker/knowledge/08-官方UI组件.md`
- 物编相关 → `.codemaker/knowledge/` 下对应文档
- 常见错误 → `.codemaker/skills/y3-lua-pipeline/references/` 下对应文档

### 3. 检查时机
| 任务类型 | 必须先阅读 |
|---------|-----------|
| 编写 Lua 代码 | `lua-issues/` + `references/api_errors.md` |
| 生成/修改 UI | `08-官方UI组件.md` + `references/ui_errors.md` |
| 生成/修改物编 | 物编知识 + `references/obj_errors.md` |
| 技能/Buff 开发 | 错题集 + 物编知识 |
| MCP 操作 | `references/mcp_safety.md` |

## ⚠️ 核心禁令

| 禁止 | 正确做法 |
|------|----------|
| 臆造 API/JSON 字段 | 查阅 `y3/` 源码验证 |
| 手写大型 JSON（300+行） | 使用脚本生成 |
| Lua 重新实现引擎功能 | 使用物编配置 |
| 手写技能栏/Buff/物品栏 | 使用官方组件 `type:17(SkillBtn)/18(BuffList)/19(BuffItem)/20(EquipSlot)` |
| **UI 坐标用左上角思维** | **Y3 原点在左下角！Y↑ X→** |
| **手写技能释放/冷却逻辑** | **使用官方技能按钮组件 type:17** |
| **生成物编/UI后直接保存** | **必须先热更再保存！否则内容丢失** |
| **手写完整 UI JSON** | **使用 `y3-ui-generator` skill 生成** |
| **凭空编造图片 ID** | **查阅 `y3-ui-beautify` 的 `IMAGE_ID_REGISTRY.md`** |
| **🔴 有现成脚本还手写新脚本** | **先查 pipeline/ 目录，使用已有工具** |

## 🚫 禁止手写脚本原则

**执行任何操作前，必须先检查是否有现成工具！**

### 检查流程

```
需要执行某项操作
    │
    ├─ 1. 先查看已加载的技能说明中是否提供了工具
    ├─ 2. 检查 .codemaker/skills/*/pipeline/ 目录
    ├─ 3. 有现成工具 → ✅ 使用，禁止手写
    └─ 4. 确认没有 → ⚠️ 才可考虑编写
```

### 🔴 触发检查点

**在以下时机必须检查是否有现成工具：**
1. 生成 UI JSON 后，准备修改时
2. 需要批量修改字段时
3. 说出"我来写个脚本"之前
4. 调用 `edit_file` 创建 `.py` 文件之前

## 🖼️ UI 生成流程（强制）

**所有 UI 面板必须通过 `y3-ui-generator` skill 生成，禁止手写 UI JSON！**

### 触发条件
用户提到以下关键词时，必须激活 `y3-ui-generator` skill：
- 生成UI、创建界面、做个面板、做个弹窗、做个HUD、做个背包、做个商店...

### 工作流（skill 内置，自动执行）
```
1. 生成结构草图（UI 树文本）
2. 生成 HTML 预览文件
3. 调用 scripts/html_to_y3_ui.py 转换为 Y3 JSON
4. 在浏览器打开预览，清理临时 HTML
```

### 关键规则
- 输出到 `maps/EntryMap/ui/<PanelName>.json`
- 坐标系：HTML 使用左上角原点，转换器自动转为 Y3 左下角原点
- 复合控件（血条/技能框/Buff列表等）使用 `data-template` 引用预制模板
- 禁止调用旧工具 `tree_to_ui_json.py` 或手写 JSON

## 🔄 引擎功能复用

**优先使用引擎系统，不要用 Lua 重新实现：**

| 功能 | 正确做法 |
|------|----------|
| 技能冷却/耗蓝 | 物编字段配置 |
| Buff 持续/叠层 | 物编字段配置 |
| 击杀奖励 | `reward_exp` / `reward_official_res_1` |
| **物品属性加成** | **物编 `attached_*` 字段，引擎自动管理（仅英雄）** |
| 技能栏 UI | 官方 `type:17` SkillBtn 技能按钮 |
| Buff 显示 | 官方 `type:18` BuffList 列表容器 + `type:19` BuffItem 单项 |
| 物品栏 UI | 官方 `type:20` EquipSlot 物品槽 |

## 🔧 Lua 代码检查

**生成或修改 Lua 文件后，必须调用 MCP 工具检查语法错误：**

```
工具: y3-helper.read_problems_lua
参数: { "pathGlob": "<lua文件路径>" }
```

示例：
- 单文件: `{ "pathGlob": "maps/EntryMap/script/main.lua" }`
- 多文件: `{ "pathGlob": ["maps/EntryMap/script/main.lua", "maps/EntryMap/script/game/xxx.lua"] }`
- 目录: `{ "pathGlob": "maps/EntryMap/script/game" }`

## 📚 知识库

| 文档 | 说明 |
|------|------|
| `.codemaker/knowledge/` | Y3 引擎知识库 |
| `.codemaker/knowledge/08-官方UI组件.md` | 官方 UI 组件指南 |
| `.codemaker/skills/y3-lua-pipeline/references/` | 常见错误速查（按类别） |

## 🔴 常见错误速查

> 详细文档见 `.codemaker/skills/y3-lua-pipeline/references/`

### 致命错误
| 错误 | 正确做法 |
|------|----------|
| 物编生成到 `.codemaker/` | 必须生成到 `maps/EntryMap/editor_table/` |
| 物编 `uid` 是整数 | 必须是字符串 `"400001"` |
| UI 按钮文本用 `text` | 按钮用 `normal_text` |
| 物品槽 `equip_bg_img` 为 `none_ref` | **必须指向 bg 子节点的 UID** |

### API 错误
| 错误 | 正确做法 |
|------|----------|
| `y3.player.with_all_players` | `y3.player_group.get_all_players():pick()` |
| `player:camera_follow_unit` | `y3.camera.set_camera_follow_unit(...)` |
| `unit:attack(target)` | `unit:attack_target(target, 0)` |

### MCP 安全
| 错误 | 正确做法 |
|------|----------|
| MCP 超时后重试 | **绝不重试！立即熔断** |
| **热更后立即保存** | **热更后等待 3 秒再保存** |
