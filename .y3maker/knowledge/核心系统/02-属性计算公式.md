# 属性计算公式

## 属性值数组结构

物编中的属性字段（如 `attached_hp_max`、`attached_attack_range` 等）使用 **5 元素数组** 格式：

```json
"attached_hp_max": [基础值, 基础加成, 增益值, 增益加成, 总加成]
```

| 索引 | 名称 | 说明 |
|------|------|------|
| 0 | **基础值** | 固定数值加成 |
| 1 | **基础加成** | 基础值的百分比加成（百分比值，如 5 = 5%） |
| 2 | **增益值** | 额外数值加成 |
| 3 | **增益加成** | 增益值的百分比加成（百分比值，如 10 = 10%） |
| 4 | **总加成** | 最终结果的百分比加成（百分比值，如 20 = 20%） |

---

## 属性计算公式

```
实际值 = (基础值 × (1 + 基础加成/100) + 增益值 × (1 + 增益加成/100)) × (1 + 总加成/100)
```

用数组索引表示：

```
实际值 = (arr[0] × (1 + arr[1]/100) + arr[2] × (1 + arr[3]/100)) × (1 + arr[4]/100)
```

> **注意**：加成字段的值是百分比整数（如 5 表示 5%），公式中需要除以 100 转换为小数。

---

## 示例

### 示例 1：简单数值加成

```json
"attached_hp_max": [100, 0, 0, 0, 0]
```

计算：`(100 × (1 + 0/100) + 0 × (1 + 0/100)) × (1 + 0/100) = 100`

**结果：生命上限 实际值 = 100**

### 示例 2：基础值 + 基础加成 50%

```json
"attached_hp_max": [100, 50, 0, 0, 0]
```

计算：`(100 × (1 + 50/100) + 0 × (1 + 0/100)) × (1 + 0/100) = 100 × 1.5 = 150`

**结果：生命上限 实际值 = 150**（100 基础值 × 150%）

### 示例 3：复合加成

```json
"attached_hp_max": [100, 20, 50, 10, 10]
```

计算：
```
基础部分 = 100 × (1 + 20/100) = 100 × 1.2 = 120
增益部分 = 50 × (1 + 10/100) = 50 × 1.1 = 55
总加成前 = 120 + 55 = 175
最终结果 = 175 × (1 + 10/100) = 175 × 1.1 = 192.5
```

**结果：生命上限 实际值 = 192.5**

### 示例 4：物品 510001 的配置

```json
"attached_hp_max": [5.0, 5.0, 5.0, 5.0, 5.0]
```

计算：
```
基础部分 = 5 × (1 + 5/100) = 5 × 1.05 = 5.25
增益部分 = 5 × (1 + 5/100) = 5 × 1.05 = 5.25
总加成前 = 5.25 + 5.25 = 10.5
最终结果 = 10.5 × (1 + 5/100) = 10.5 × 1.05 = 11.025
```

**结果：该属性字段折算值 = 11.025**（不是单位最终面板增加值）

> **重要区分（仅物品场景）**：示例 1-3 可直接视为单位属性字段的实际值计算。
> 对于示例 4 这类，公式结果仅表示物品字段折算值；单位拿到物品后会由引擎并入单位属性体系计算，无法仅凭单个物品字段推断单位最终增加量。

---

## 常见属性字段

| 字段 | 属性名 | 说明 |
|------|------|------|
| `attached_hp_max` | 生命上限 | 单位的最大生命值 |
| `attached_mp_max` | 魔法上限 | 单位的最大技能资源 |
| `attached_attack_range` | 攻击范围 | 单位可以攻击攻击范围内的可见单位，当普攻技能替换为自定义类型时，会使用技能的释放范围(双向绑定，修改技能的释放范围属性会导致单位的攻击范围同步变化，反之亦然) |
| `attached_attack_interval` | 攻击间隔 | 单位两次普通攻击之前间隔的秒数，当普攻技能替换为自定义类型时，会使用技能的冷却时间(双向绑定，修改技能的冷却时间属性会导致单位的攻击范围同步变化，反之亦然) |
| `attached_attack_phy` | 物理攻击 | 单位的物理攻击力 |
| `attached_attack_mag` | 法术攻击 | 单位的法术攻击力 |
| `attached_defense_phy` | 物理防御 | 单位的物理防御力 |
| `attached_defense_mag` | 法术防御 | 单位的法术防御力 |
| `attached_attack_speed` | 攻击速度 | 单位的攻击速度百分比，局内显示的实际攻速为:1/单位当前普通攻击技能冷却时间*攻击速度 |
| `attached_cd_reduce` | 冷却缩减 | 单位技能进入cd时减少部分冷却时间 |
| `attached_strength` | 力量（单值，非数组） | 力量 |
| `attached_agility` | 敏捷（单值，非数组） | 敏捷 |
| `attached_intelligence` | 智力（单值，非数组） | 智力 |
| `attached_critical_chance` | 暴击几率 | 单位普通攻击有概率造成额外伤害 |
| `attached_critical_dmg` | 暴击伤害 | 发生暴击时，造成的暴击伤害倍数 |
| `attached_dodge_rate` | 闪避率 | 单位躲避其他单位普通攻击的概率 |
| `attached_hit_rate` | 命中率 | 单位普通攻击命中其他单位的概率 |
| `attached_hp_rec` | 生命恢复 | 单位的每秒生命恢复数值 |
| `attached_mp_rec` | 魔法恢复 | 单位的每秒技能资源恢复数值 |
| `attached_pene_mag` | 魔法穿透 | 穿透敌人法术防御力。先计算固定穿透，再计算百分比穿透 |
| `attached_pene_phy` | 物理穿透 | 穿透敌人物理防御力。先计算固定穿透，再计算百分比穿透 |
| `attached_pene_mag_ratio` | 魔法穿透比例 | 百分比穿透敌人法术防御力。先计算固定穿透，再计算百分比穿透 |
| `attached_pene_phy_ratio` | 物理穿透比例 | 百分比穿透敌人物理防御力。先计算固定穿透，再计算百分比穿透 |
| `attached_vampire_mag` | 法术吸血 | 造成法术伤害后可以恢复自身生命值 |
| `attached_vampire_phy` | 物理吸血 | 造成物理伤害后可以恢复自身生命值 |
| `attached_dmg_reduction` | 受伤减免 | 百分比降低受到的伤害 |
| `attached_extra_dmg` | 伤害加成 | 百分比提高造成的伤害 |
| `attached_healing_effect` | 被治疗加成 | 当单位受到治疗效果时，提升治疗量 |
| `attached_ori_speed` | 移动速度 | 单位每秒移动的距离。 |
| `attached_vision_true` | 真实视野 | 单位所能侦测到隐身单位的范围 |
| `attached_vision_rng` | 白天视野 | 单位在白天可以看到（驱散战争迷雾）的范围 |
| `attached_vision_night` | 夜晚视野 | 单位在夜晚可以看到（驱散战争迷雾）的范围 |
| `attached_vision_sector_rng` | 白天扇形视野半径 | 单位在白天拥有的扇形视野半径。 |
| `attached_vision_sector_night` | 夜晚扇形视野半径 | 单位在夜晚拥有的扇形视野半径。 |
| `attached_vision_sector_angle_day` | 白天扇形视野夹角 | 单位在白天拥有的扇形视野夹角。 |
| `attached_vision_sector_angle_night` | 夜晚扇形视野夹角 | 单位在夜晚拥有的扇形视野夹角。 |
| `attached_hp_rec_percent` | 百分比生命恢复 | 单位的每秒生命恢复百分比数值 |

> 以上字段基于 y3 lualib 的单位属性枚举补全（见 `maps/EntryMap/script/y3/game/const.lua` 中的 `UnitAttr`）。
---

## 引擎自动属性/技能绑定

**物品属性和技能由引擎自动管理，无需 Lua 代码处理！**

> ⚠️ **仅限英雄单位**：只有英雄类型的单位才会自动获得物品属性和技能！

### 自动绑定行为

| 事件 | 引擎行为 |
|------|----------|
| **英雄**获得物品 | 自动获得物品的 `attached_*` 属性加成 + 技能 |
| **英雄**失去物品 | 自动移除物品的 `attached_*` 属性加成 + 技能 |
| 非英雄单位获得物品 | ❌ 不会自动获得属性和技能 |

### 物品技能字段

| 字段 | 说明 | 格式 |
|------|------|------|
| `attached_ability` | **主动技能** | `{"__tuple__": true, "items": [[技能ID, 槽位], ...]}` |
| `attached_passive_abilities` | **被动技能** | `{"__tuple__": true, "items": [[技能ID, 槽位], ...]}` |

### 示例：带技能的物品

```json
{
    "attached_ability": {
        "__tuple__": true,
        "items": [[100001001, 1]]  // 主动技能 100001001，槽位 1
    },
    "attached_passive_abilities": {
        "__tuple__": true,
        "items": [[100001002, 1]]  // 被动技能 100001002，槽位 1
    }
}
```

> **正确做法**：在物编中配置好物品的 `attached_*` 属性字段和技能字段即可（英雄单位）
> 
> **错误做法**：用 Lua 监听装备事件手动加减属性或添加技能

---

## 注意事项

1. **单值字段** vs **数组字段**
   - 部分属性（如 `attached_strength`）是单值，直接加成
   - 部分属性（如 `attached_hp_max`）是 5 元素数组，按公式计算

2. **百分比表示**
   - 加成值使用百分比整数表示：`5 = 5%`，`50 = 50%`
   - 公式计算时需要除以 100 转换为小数

3. **快速配置技巧**
   - 只需要固定数值加成：`[数值, 0, 0, 0, 0]`
   - 只需要总百分比加成：`[0, 0, 0, 0, 百分比]`
   - 基础值 + 百分比加成：`[基础值, 百分比, 0, 0, 0]`

4. **属性自动生效**
   - 物品属性由引擎自动管理
   - 获得物品时自动加属性，失去物品时自动减属性
   - 无需编写任何代码逻辑

---

