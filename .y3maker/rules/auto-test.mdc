---
alwaysApply: false
---
# 自动化测试执行规则

> ⚠️ **启用条件**：仅当用户明确指示执行自动化测试（如"开始测试"、"执行测试计划"、"跑测试"等）时激活本规则。日常开发、写代码、生成物编/UI 时**不适用**。

---

## 📌 Gate 准入与测试案生成

> **Gate 准入流程（Gate 1~3）和测试案生成规范已迁移到 `y3-game-spec` 编排器。**
> 详见：`skills/y3-game-spec/SKILL.md` 阶段 3 + `skills/y3-game-spec/gen-test-plan.md`
>
> 本文件仅定义**测试执行期间的纪律规则**（通过 Gate 后的行为规范）。

---

## 🔴 核心原则：不停、不跳、不问、不磨、不记仇

```
┌─────────────────────────────────────────────────────────────────────┐
│  自动化测试期间，AI 的行为准则：                                      │
│                                                                     │
│  1. 不停  — 除非环境崩溃或 MCP 熔断，否则不得主动停止执行             │
│  2. 不跳  — 每个测试点必须执行到出结果，不得标记 NOT_TESTED 跳过       │
│  3. 不问  — 如非必要不得中断询问用户，自行决策并继续（例外见下方）     │
│  4. 不磨  — 单个测试点超 3 轮或 10 次工具调用 → 立即 KNOWN ISSUE     │
│  5. 熔断即停 — MCP 熔断触发 → 立即终止测试，生成测试报告              │
│  6. 不记仇 — 每次测试独立执行，不受历史测试结果影响（见规则 7）        │
│  7. 不漏报 — 报告测试点必须与计划 1:1 对齐，生成前强制交叉校验（规则6.5）│
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**"不问"原则的例外**（仅以下 3 种场景允许询问用户）：

| # | 场景 | Gate/规则 | 原因 |
|---|------|-----------|------|
| 1 | 测试模式选择 | Gate 2.5 | 模式决定后续所有行为 |
| 2 | 清理范围确认 | Gate 3 | 误删可能造成不可逆损失 |
| 3 | Blocking 测试点 3 次修复失败 | 规则 3 | 需要用户决定是否终止测试 |

> **⏱️ 单点熔断（硬性约束）**：单个测试点的修复尝试不得超过 **3 轮（Round 1~3）或累计 10 次工具调用**（以先到者为准）。超限后**立即**标记 🚫 KNOWN ISSUE 并跳到下一个测试点。不得反复尝试相同思路，不得无限深挖根因。

> **🔴 MCP 熔断（最高优先级）**：当 MCP 工具调用超时或连续失败 2 次时，说明基础工具链已不可用。此时**立即终止整个测试流程**，生成测试报告（TestReport.md）。报告中已执行用例标注实际结果，未执行用例统一标记为 🚫 BLOCKED（MCP 熔断）。详见 `mcp-rules.mdc` 熔断规则。

---

## 📋 规则 1：每个测试点必须执行到尾

### 强制执行

- 测试计划中的**每一个**测试用例（含子项如 T-27a、T-27b 等）都必须实际执行
- 执行 = 调用工具（`execute_lua`、`bot_click`、`bot_hotkey`、`capture_screenshot` 等）并获取结果
- **禁止**仅凭推测标记结果，必须有工具调用的实际输出作为判定依据

### 结果判定标准

| 结果 | 含义 | 要求 |
|------|------|------|
| ✅ PASS | 测试通过 | 必须有工具输出证据 |
| ❌ FAIL | 测试失败 | 必须有工具输出证据 + 失败原因 |
| ⚠️ PARTIAL | 部分通过 | 必须说明哪部分通过、哪部分失败 |
| 🚫 BLOCKED | 环境不允许 | 仅限：游戏崩溃、MCP 断连、编辑器无响应等硬性障碍 |

### 禁止的结果标记

| ❌ 禁止 | 原因 |
|---------|------|
| 🔲 NOT_TESTED | 不存在"未测试"，要么测要么 BLOCKED |
| "需要 bot_click" | 你有 bot_click 工具，直接用 |
| "需要 bot_hotkey" | 你有 bot_hotkey 工具，直接用 |
| "需要截图对比" | 你有 capture_screenshot，直接截 |
| "依赖 T-XX" | 先去执行 T-XX，然后回来继续 |

---

## 📋 规则 2：失败重试机制（3 轮 or 10 次工具调用，先到先停）

> ⏱️ **熔断约束**：以下整个流程不得超过 **3 轮修复或累计 10 次工具调用**（以先到者为准）。超限后无论在哪一步，立即标记 🚫 KNOWN ISSUE 并跳到下一测试点。

当测试用例失败时，执行以下流程：

```
测试失败
    │
    ▼
┌─────────────────────────────────────┐
│ 分析失败原因                         │
│ - 查看错误日志 / Trace               │
│ - 查阅 lua-issues/ 错题本            │
│ - 搜索 y3 源码确认 API               │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│ 修复尝试 (最多 3 次)                 │
│                                     │
│ Round 1: 修改代码 → 热更 → 重测     │
│   ├─ 通过 → ✅ PASS（记录修复过程） │
│   └─ 失败 → 进入 Round 2            │
│                                     │
│ Round 2: 换方案 → 热更 → 重测       │
│   ├─ 通过 → ✅ PASS（记录修复过程） │
│   └─ 失败 → 进入 Round 3            │
│                                     │
│ Round 3: 最后尝试 → 热更 → 重测     │
│   ├─ 通过 → ✅ PASS（记录修复过程） │
│   └─ 失败 → 标记 ❌ UNRESOLVED      │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│ 结果处理                             │
│                                     │
│ 修复成功 → 记入 lua-issues/ 错题本   │
│ 修复失败 → 标记 ❌ UNRESOLVED        │
│           记录 3 次尝试的方案和结果   │
│           判定是否卡流程（见规则 3）  │
└─────────────────────────────────────┘
```

### 修复记录格式

每次修复都必须记录：

```markdown
| 轮次 | 测试点 | 尝试方案 | 结果 |
|------|--------|----------|------|
| R1 | T-XX | 修改了 xxx | 仍失败：原因 |
| R2 | T-XX | 改用 yyy 方案 | 仍失败：原因 |
| R3 | T-XX | 尝试 zzz | ✅ 修复成功 / ❌ 仍失败 |
```

---

## 📋 规则 3：流程阻断判定

### MCP 熔断（最高优先级，高于 Blocking）

**定义**：MCP 工具链不可用（超时或连续失败 2 次），无法执行任何测试操作。

```
触发条件（满足任一即触发）：
  - MCP 调用超时
  - 同一 MCP 工具连续调用失败 2 次
```

**处理方式**：**立即终止测试**，生成 TestReport.md，未执行用例标记 🚫 BLOCKED（MCP 熔断）。

### 卡流程（Blocking）测试点

**定义**：该测试点失败会导致后续测试点无法执行。

```
判定条件（满足任一即为 Blocking）：
  - 游戏无法启动或崩溃
  - 核心对象无法创建（英雄、怪物不存在）
  - 主流程断裂（选择英雄后无法进入战斗）
  - Lua 代码存在语法错误导致脚本完全无法加载
```

**处理方式**：

| 修复结果 | 操作 |
|----------|------|
| 3 次内修复成功 | 继续执行后续测试 |
| 3 次修复失败 | **提前结束测试**，生成当前报告，标注终止原因 |

### 不卡流程（Non-blocking）测试点

**定义**：该测试点失败不影响后续测试点。

```
典型场景：
  - UI 文本显示不正确（不影响逻辑）
  - 某个技能释放异常（不影响其他技能测试）
  - 装备效果数值偏差（不影响死亡/复活测试）
  - 视觉效果缺失（不影响功能验证）
```

**处理方式**：

| 修复结果 | 操作 |
|----------|------|
| 3 次内修复成功 | 记录并继续 |
| 3 次修复失败 | 标记 ❌ UNRESOLVED，**跳过继续执行后续测试** |

---

## 📋 规则 4：执行纪律

### 禁止行为

| ❌ 禁止 | 说明 |
|---------|------|
| 主动停止游戏再生成报告 | 所有测试执行完毕后才能停止游戏 |
| "差不多了"心态收尾 | 必须逐个执行到最后一个用例 |
| 把可执行的测试标为"未测试" | 有工具就必须用 |
| 批量跳过桌面交互类测试 | `bot_click`/`bot_hotkey` 都是可用工具 |
| 中途询问用户"要不要继续" | 除非遇到 Blocking 失败且 3 次修复无果 |
| 因为"复杂"或"耗时"而跳过 | 复杂不是跳过的理由 |

### 允许行为

| ✅ 允许 | 说明 |
|---------|------|
| 自动决定测试执行顺序 | 可以调整顺序以提高效率 |
| 合并相似测试点 | 一次操作验证多个相关用例 |
| 快速重启游戏 | 用 `quick_restart` 重置状态 |
| 使用 `execute_lua` 模拟操作 | 当桌面自动化确实无法完成时的替代方案 |
| 标记 BLOCKED | 仅限硬性环境障碍（MCP 断连、游戏崩溃等） |

> 允许询问用户的场景见"不问"原则例外表（Gate 2.5 / Gate 3 / Blocking 3 次失败）。

---

## 📋 规则 5：上下文管理与续接机制

### 核心思路

**首要目标：一次会话跑完全部测试。** AI 无法自己新建会话，续接需要用户手动介入，会打断测试流程。因此必须从一开始就控制上下文消耗，确保一次跑完。

### 三级响应机制

```
┌─────────────────────────────────────────────────────────────────────┐
│ 上下文使用 ≤ 40%   → 🟢 正常模式                                    │
│   正常输出日志、详细记录每个测试点                                    │
├─────────────────────────────────────────────────────────────────────┤
│ 上下文使用 40%~80% → 🟡 精简模式                                    │
│   - 合并相似测试项（一次工具调用验证多个点）                          │
│   - 省略中间过程描述，只记录结果                                     │
│   - 日志只取关键行，不取全部                                         │
│   - 不截图，改用 get_logs / execute_lua 验证                         │
│   - 每个测试点输出控制在 3 行以内                                    │
├─────────────────────────────────────────────────────────────────────┤
│ 上下文使用 > 80%   → 🔴 续接模式（最后手段）                         │
│   - 立即生成轻量续接文档（≤30 行）                                   │
│   - 提示用户新建会话继续                                             │
│   - ⚠️ 这意味着测试被打断，应尽一切努力避免进入此模式                 │
└─────────────────────────────────────────────────────────────────────┘
```

### 🔵 全局省上下文策略（从第一个测试点就生效）

不等进入精简模式，以下策略**全程适用**：

| 策略 | 说明 |
|------|------|
| **合并验证** | 相关测试点尽量合并到一次 `execute_lua` 调用中 |
| **get_logs 限制** | 默认 `limit=10`，只在排错时用更大值 |
| **截图按需** | 仅在必须视觉验证时截图，能用日志/数据验证的不截图 |
| **不重复读文件** | 已读过的文件不再重读，依赖记忆 |
| **输出紧凑** | 每个测试点的回复控制在 5 行以内 |
| **bot_wait 最小化** | 只等必要的时间，不多等 |

### 🟡 精简模式规则

进入精简模式后，执行方式发生以下变化：

| 维度 | 正常模式 | 精简模式 |
|------|----------|----------|
| **测试项** | 逐个执行 | 合并相关项，一次 execute_lua 验证多个 |
| **日志获取** | `get_logs` limit=20~50 | `get_logs` limit=5 或不获取 |
| **截图** | 按需截图 | 不截图，改用日志验证 |
| **输出** | 详细描述 | 仅 `T-XX: ✅/❌ 一句话原因` |
| **修复尝试** | 最多 3 轮 | 最多 1 轮，失败即 KNOWN ISSUE |
| **等待** | bot_wait 按需 | 压缩等待时间 |

**精简模式示例输出**：
```
T-28: ✅ 波次UI+金币UI+等级UI 全部存在
T-29: ✅ EXP条存在 (exp_area + label_exp)
T-30: ✅ Buff列表存在
T-31: ⚠ 背包面板存在但物品数为0
```

### 🔴 续接模式规则

**续接文档必须轻量化**，控制在 30 行以内，只写恢复执行必需的信息：

```markdown
# 测试续接
时间: YYYY-MM-DD HH:MM | 计划: AutoTestPlan.md | 模式: 开发+测试

## 结果 (已完成 N/M)
T-01:✅ T-02:✅ T-03:✅ T-04:⚠(物品数0) T-05:❌(3次失败)

## 恢复点
下一个: T-06 | 游戏状态: 运行中 | Phase: 3 | 波次: 5/10

## 关键修复
- HP条: 需同时设置type_41+type_39
- 金币: 用get_attr("gold")不是get_res_num
```

### 强制规则

```
上下文不足时：
  ❌ 禁止草草收尾生成不完整报告
  ❌ 禁止把剩余用例标为 NOT_TESTED
  ❌ 禁止花大量上下文写长篇总结（总结本身也消耗上下文！）
  ✅ 精简模式下优先完成当前 Phase 的所有测试点
  ✅ 续接文档控制在 30 行以内
  ✅ 提示用户新建会话继续
```

### 新会话续接

新会话收到"继续执行测试"指令时：
1. 读取 `.codemaker/memory/test-continuation.md`
2. 读取 `auto-test.mdc`（激活测试规则）
3. 从上次中断的测试点继续执行（**不重复已完成的测试**）
4. 最终报告合并所有会话的测试结果

---

## 📋 规则 6：报告生成时机

```
测试报告生成时机：
  ├─ 所有测试用例执行完毕（正常结束）
  ├─ Blocking 测试点 3 次修复失败（提前终止）
  └─ MCP 熔断触发（立即终止）

禁止在测试进行中生成最终报告！（MCP 熔断除外，熔断 = 工具链不可用 = 必须立即终止）
```

### 报告必须包含

- 每个测试点的实际执行证据（工具调用输出摘要）
- 修复记录（所有 Round 1~3 的尝试）
- UNRESOLVED 用例的 3 次尝试详情
- BLOCKED 用例的环境障碍说明

---

## 📋 规则 6.5：报告与测试计划交叉校验（强制）

> 🔴 **这是最终报告的质量门禁，在生成 TestReport.md 之前必须执行。**
> 📜 **前置依赖**：测试案本身必须先通过 `doc-consistency.mdc` 的一致性校验（策划案↔测试案双向对齐），确保不存在"无策划依据"的测试项。如果报告中出现大量"设计差异/不适用"标注，说明测试案未通过一致性校验，应先修正测试案再重新执行测试。

### 核心约束

**最终测试报告中的测试点必须与 AutoTestPlan.md 中定义的测试点 1:1 对应，不得多、不得少、编号必须完全匹配。**

### 校验流程

```
生成报告前 → 执行交叉校验
    │
    ├── 1. 从 AutoTestPlan.md 提取所有测试编号（含子项如 T-26a、T-27b 等）
    │      → 得到 plan_ids 列表
    │
    ├── 2. 从报告草稿中提取所有测试编号
    │      → 得到 report_ids 列表
    │
    ├── 3. 比对差异
    │      missing = plan_ids - report_ids    （计划有但报告缺失）
    │      extra   = report_ids - plan_ids    （报告有但计划没有）
    │
    ├── 4. 数量校验
    │      len(report_ids) 必须 == len(plan_ids)
    │
    └── 5. 判定
           ├─ missing 为空 且 extra 为空 且 数量一致 → ✅ 校验通过，可生成报告
           └─ 任一不为空 或 数量不一致 → ❌ 校验失败，必须补齐后重新校验
```

### 校验失败的处理

| 情况 | 处理方式 |
|------|----------|
| **计划有但报告缺失** | 补上缺失的编号，标记实际执行结果（如已执行）或 🚫 BLOCKED + 原因 |
| **报告有但计划没有** | 删除多余项（不得自创测试编号） |
| **数量不一致** | 逐行核对，找出遗漏/重复，修正后重新计数 |

### 禁止行为

| ❌ 禁止 | 说明 |
|---------|------|
| 跳过校验直接生成报告 | 校验是报告的前置步骤，不可省略 |
| 合并子项后只写父项 | T-27a~T-27g 合并执行可以，但报告中 7 个编号必须逐一出现 |
| 自创新编号 | 报告中只能出现 AutoTestPlan.md 中定义的编号 |
| 用"等"/"..." 省略 | 每个编号必须显式列出 |

### 校验日志格式

校验结果必须写入报告末尾：

```markdown
## 📋 交叉校验结果
| 项目 | 值 |
|------|-----|
| 计划测试点数 | N |
| 报告测试点数 | N |
| 缺失 | 0 |
| 多余 | 0 |
| 校验结果 | ✅ 通过 |
```

---

## 📋 规则 7：测试独立性（不记仇原则）

### 核心规则

**每次测试执行都是独立的，禁止因为历史测试结果而预先跳过或降级任何测试点。**

### 禁止行为

| ❌ 禁止 | 说明 |
|---------|------|
| 因为"上次 T-XX 失败"就不测 T-XX | 上次失败不代表这次会失败，环境/代码可能已变化 |
| 因为"上次标记为 KNOWN ISSUE"就直接跳过 | KNOWN ISSUE 是上次的结论，本次必须重新验证 |
| 预设"这个测试点很难通过"而简化测试 | 每个测试点必须按完整标准执行 |
| 参考历史失败原因直接放弃尝试 | 可以参考历史信息辅助排查，但不能用来跳过 |

### 允许行为

| ✅ 允许 | 说明 |
|---------|------|
| 参考 `lua-issues/` 错题本**加速排查** | 历史经验可以帮助更快定位问题 |
| 参考上次修复方案**作为优先尝试** | 优先用已知有效的方案，但不能跳过测试本身 |
| 在报告中标注"与上次相同问题" | 结果对比是有价值的信息 |

### 原则

```
历史记录的正确用法：加速排查、辅助修复
历史记录的错误用法：预判跳过、降级测试
```

每次测试的 PASS/FAIL/BLOCKED 判定必须基于**本次实际执行的工具调用输出**，不得基于历史记忆。

---

*最后更新: 2026-04-10*
*适用范围: 仅自动化测试执行*
