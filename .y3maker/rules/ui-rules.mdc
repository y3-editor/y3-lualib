# UI 开发规则

> Y3 UI 系统的特殊规则和常见陷阱

## 🔴 坐标系（最重要！）

**Y3 UI 原点在左下角！Y 轴向上！**

```
     Y↑
      │
      │
      └────→ X
    (0,0)
```

- ❌ **错误**：用左上角思维写坐标
- ✅ **正确**：记住 Y 值越大越靠上

## 🎯 官方组件（必须使用）

| type | 名称 | 用途 | 禁止替代方案 |
|------|------|------|-------------|
| 17 | SkillBtn | 技能按钮 | ❌ 手写技能释放/冷却逻辑 |
| 18 | BuffList | Buff列表容器 | ❌ 手写 Buff 显示 |
| 19 | BuffItem | Buff单项 | - |
| 20 | EquipSlot | 物品槽 | ❌ 手写物品栏 |

## ⚠️ 常见字段错误

| 控件 | ❌ 错误字段 | ✅ 正确字段 |
|------|------------|------------|
| 按钮文本 | `text` | `normal_text` |
| 物品槽背景 | `equip_bg_img: "none_ref"` | `equip_bg_img: "{bg子节点UID}"` |
| 进度条值 | 直接设置 value | 先 `set_max_...` 后 `set_current_...` |

## 📋 UI 生成流程

```
用户需求 → y3-ui-pipeline（统一入口）
    │
    ├─ 需要新建 UI → y3-ui-generator（生成 JSON）
    │       └─ 草图 → HTML 预览 → 转换 JSON
    │
    └─ 需要 Lua 代码 → y3-lua-pipeline（API 参考）
```

### 禁止手写 UI JSON！

UI JSON 文件通常 200+ 行，必须使用 `y3-ui-generator` 技能通过 HTML 转换生成。

## 📚 参考文档

| 文档 | 路径 |
|------|------|
| UI 字段规范 | `knowledge/UI系统/01-字段规范.md` |
| 官方组件指南 | `knowledge/UI系统/03-官方组件.md` |
| 控件适配规则 | `knowledge/UI系统/07-控件适配规则.md` |

