# 事件
```lua
    单位1:增加经验(100)

    y3.游戏:事件("单位-获得经验后", function(trg, data)
        调试输出("这里先触发")
    end)


    单位1:事件("单位-获得经验后", function(trg, data)
        调试输出("这里后触发")
    end)

    单位2:事件("单位-获得经验后", function(trg, data)
        调试输出("这里不会触发")
    end)
    
```